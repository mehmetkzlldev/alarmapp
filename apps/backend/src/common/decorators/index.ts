// `CurrentUser` is the canonical param decorator under `src/common/auth/`.
// This barrel exposes only the route-metadata decorators owned by the
// common platform layer.
export * from './public.decorator';
export * from './roles.decorator';
export * from './premium.decorator';
